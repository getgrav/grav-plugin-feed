form:
  fields:
    tabs:
      fields:
        blog:
          type: tab

          fields:
            header.feed.limit:
              type: text
              label: PLUGIN_FEED.LIMIT
              default: 10
              validate:
                type: int

            header.feed.title:
              type: text
              label: PLUGIN_FEED.TITLE

            header.feed.description:
              type: text
              label: PLUGIN_FEED.DESCRIPTION
